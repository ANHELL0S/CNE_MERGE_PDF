services:
  cne-merge-pdf:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: cne-merge-pdf
    ports:
      - "3010:3010"
    restart: unless-stopped
    environment:
      - PORT=3010
      - NODE_ENV=production
